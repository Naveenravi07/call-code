{
  "dependencies": {
    "@nestjs/mapped-types": "*"
  },
    "name": "codecall",
    "private": true,
    "scripts": {
        "build": "turbo build",
        "lint": "turbo lint",
        "format": "prettier --write \"**/*.{ts,tsx,md}\"",
        "dev": "turbo dev",
        "dev:main": "turbo run dev --filter=web --filter=callcodeserver"
    },
    "devDependencies": {
        "prettier": "^3.2.5",
        "turbo": "^2.0.9",
        "typescript": "^5.4.5"
    },
    "packageManager": "pnpm@8.15.6",
    "engines": {
        "node": ">=18"
    }
}
